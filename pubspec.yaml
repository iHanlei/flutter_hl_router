name: hl_router
description: A declarative Navigator 1.0 routing layer with parameters, guards, and transitions.
version: 1.0.1

environment:
  sdk: ^3.9.0
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
